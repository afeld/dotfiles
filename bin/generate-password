#!/bin/sh

# Usage:
#
#   generate-password <num-chars>
#
# `num-chars` defaults to 40.

set -euf -o pipefail

NUM_CHARS=${1-40}
# https://gist.github.com/earthgecko/3089509
cat /dev/urandom | env LC_CTYPE=C tr -dc 'a-zA-Z0-9%^&*()_+?><~\`;' | fold -w $NUM_CHARS | head -n 1
