[user]
	email = afeld@github.com
	name = Aidan Feldman
[core]
	excludesfile = ~/.gitignore
	editor = vim
	autocrlf = input
	precomposeunicode = true
[color]
	ui = true
[merge]
	conflictstyle = diff3
	keepBackup = false
	tool = custom
[github]
	user = afeld
[alias]
	bclean = clean-branches
	branches = latest-branches
	latest = latest-tag
	lbclean = clean-local-branches
	lg = pretty-log
	mw = merge-no-whitespace
	um = checkout-upstream-master
	co = checkout
	pr = create-pull-request
	feature = create-feature-branch
[branch]
	autosetupmerge = true
[mergetool "custom"]
	cmd = /Applications/p4merge.app/Contents/Resources/launchp4merge "$PWD/$BASE" "$PWD/$REMOTE" "$PWD/$LOCAL" "$PWD/$MERGED"
	keepTemporaries = false
	trustExitCode = false
	keepBackup = false
[push]
	# only push the current branch
	default = upstream
[hub]
	protocol = https
[credential]
	helper = osxkeychain

[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[url "https://github.com"]
	insteadOf = git://github.com
